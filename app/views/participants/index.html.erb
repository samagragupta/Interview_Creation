<%= form_for(@participant, remote: true) do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %><br>
  <br>
  <%= f.label :email %><br>
  <%= f.text_field :email %>
  <br>
  <%=f.submit%>
<% end %> 

<%= link_to 'Back', :home_index %>
